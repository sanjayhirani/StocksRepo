name: Apex Market Scan
on:
  schedule:
    - cron: '0 14 * * 1-5'
  workflow_dispatch:

jobs:
  scan_and_alert:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install required fonts
        run: |
          run: |
          sudo apt-get update
          sudo apt-get install -y libcairo2-dev pkg-config python3-dev
          sudo apt-get install -y fonts-lato fonts-open-sans
          sudo fc-cache -f -v
          
      - name: Install Dependencies
        run: |
          pip install --upgrade pip
          pip install Pillow yfinance pandas numpy gspread oauth2client google-generativeai requests lxml svglib reportlab
          
      - name: Run Apex Scanner
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GOOGLE_CREDS: ${{ secrets.GOOGLE_CREDS }}
        run: python Apex_Stock_Scanner.py
